Ext.define('Module.Hyscan.MaterialAlgo.Tools', {
    singleton: true,

    requires: [
        'Soul.util.ObjectView'
    ],

    constructor: function () {
        this.callParent(arguments);
    }
});